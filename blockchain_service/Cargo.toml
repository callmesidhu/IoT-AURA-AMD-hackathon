[package]
name = "blockchain_service"
version = "0.1.0"
edition = "2024"

[dependencies]
ethers = "2.0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
dotenv = "0.15"
axum = "0.7"
serde_json = "1"